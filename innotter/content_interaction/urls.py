from rest_framework.routers import DefaultRouter

# from .views import FollowingViewSet
#
# router = DefaultRouter()
# router.register(r'follows', FollowingViewSet, basename='follow')
# urlpatterns = router.urls

